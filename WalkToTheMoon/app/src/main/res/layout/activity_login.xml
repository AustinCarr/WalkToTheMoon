<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".Login"
    android:background="@drawable/main_background_gradient">
    <ImageView
        android:id="@+id/image_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/title"
        android:layout_marginTop="20dp"/>
    <EditText
        android:id="@+id/text_edit_login"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/image_title"
        android:layout_marginTop="50dp"
        android:hint="@string/login"
        android:inputType="textEmailSubject" />
    <EditText
        android:id="@+id/text_edit_password"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/text_edit_login"
        android:layout_marginTop="15dp"
        android:hint="@string/password"
        android:inputType="textPassword"/>
    <Button
        android:id="@+id/button_login"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/button_login"
        android:layout_below="@+id/text_edit_password"
        android:layout_margin="25dp"
        android:background="@drawable/login_button_background_gradient"
        android:shadowRadius="0"
        android:shadowDx="0"
        android:shadowDy="0"
        android:textColor="#3e3860">
       </Button>
    <Button
        android:id="@+id/button_create_accnt"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/button_create_accnt"
        android:layout_alignParentBottom="true"
        android:layout_margin="5dp"
        android:textColor="#3e3860"
        android:background="#00ffffff">
        </Button>
</RelativeLayout>
